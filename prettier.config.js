/**
 * @see https://prettier.io/docs/en/options
 * @type {import("prettier").Config}
 */
const config = {
    printWidth: 120,
    tabWidth: 4,
    singleQuote: true,
    arrowParens: 'avoid',
    trailingComma: 'all',
    semi: true,
};

module.exports = config;
